---
import Layout from '../layouts/Layout.astro';
---
<Layout title="Archives">
    <section id="programme" class="pt-28 relative z-10">
        <div class="container m-auto px-6 py-20 md:px-12 lg:px-20">
            <div class="m-auto text-center lg:w-7/12">
                <h2 class="mb-16 text-4xl font-bold text-white md:text-4xl lg:text-6xl">
                    Revivez en images les éditions passées
                </h2>
            </div>
            <div class="relative pb-12">
                <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
                    <!-- <a data-fslightbox="gallery" href="{{ vich_uploader_asset(image, 'image_file') }}">
                        <img class="h-auto max-w-full rounded-lg" src="{{ vich_uploader_asset(image, 'image_file') }}" alt="{{ image.id }}">
                    </a> -->
                </div>
    
            </div>
        </div>
    </section>
</Layout>
